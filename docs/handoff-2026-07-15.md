# ごみニコ（gomi-nico.jp）引継ぎドキュメント（2026-07-15時点）

> 別チャット・別セッションでの作業引き継ぎ用。前回の引継ぎ書は `handoff-2026-07-10.md`。

---

## プロジェクト概要

- **サービス名**: ごみニコ（2026-07-15に「ごみ分別ガイド」から改称・全ページ統一済み）
- **旧名称**: ごみ分別ガイド／志木市ごみ分別ガイド（JSON-LDのalternateNameとして意図的に残存）
- **ドメイン**: gomi-nico.jp（**公開済み・HTTPS有効**）
- **運営**: 株式会社ユウセナ（法人番号 7030001169874、埼玉県志木市）
- **連絡先**: contact@gomi-nico.jp ／ miyazawa.taihei@yousena.com
- **インフラ**: Cloudflare Pages（GitHub連携で自動デプロイ）
- **リポジトリ**: C:\Users\user\Claude\Projects\gomi-nico → github.com/yousena/gomi-nico
- **方針**: 自治体別マルチテナント。無料サービス、広告収入（AdSense＋楽天アフィリエイト）で運用。第1号自治体は志木市

## インフラ・計測の現状（すべて2026-07-15に確認済み）

| 項目 | 状態 |
|---|---|
| gomi-nico.jp | ✅ HTTPS有効・正常配信（CNAME @ → gomi-nico.pages.dev, Proxied） |
| www.gomi-nico.jp | ✅ 301で apex へリダイレクト（Redirect Rules「WWW to Root」+ Preserve query string） |
| gomi-nico.pages.dev | ✅ Bulk Redirectsで gomi-nico.jp へ301。**Include subdomainsは無効**（プレビュー環境を残すため） |
| テスト環境 | main以外のブランチにpush → `<ハッシュ>.gomi-nico.pages.dev` のプレビューが使える |
| GTM | GTM-MNH8H4L2（全ページ設置済み） |
| GA4 | GTM経由。ストリームURLは gomi-nico.jp に更新済み。**内部トラフィック除外を設定**（自宅IP方式。IP変動時は要更新。動作検証は「iPhoneのWi-Fi切って4G/5Gでアクセス→リアルタイムに出るか」で行う） |
| Microsoft Clarity | GTM経由で設定済み |
| Search Console | ✅ ドメインプロパティで所有権確認済み。sitemap.xml送信済みだが「取得できませんでした」表示 → **登録直後の一時表示の可能性が高く様子見中**。1〜2日後に成功に変わらなければ削除→再送信 |

## サイト構成

```
public/
  index.html       トップLP（2026-07-10〜15に全面リニューアル）
  shiki/index.html 志木市アプリ（データ・機能はscript.js + data_shiki.json）
  script.js        全ロジック（都市検出・メタ動的生成・カレンダー・検索等）
  data_shiki.json  志木市データ（20カテゴリ・93品目・10地区・FAQ）
  about/privacy/ads.html  法的ページ
  sitemap.xml / robots.txt / manifest.json / sw.js（PWA, キャッシュ名 gomi-nico-v2）
  icons/           ごみ種別SVG12種 + PWAアイコンPNG
docs/              引継ぎ書・ロードマップ・進捗メモ類
```

## 名称・タイトル規則（2026-07-15決定）

- **サービス名は「ごみニコ」で統一**（サイト表示・OGP・JSON-LD・PWA・GA4）。「志木市」はサービス名に冠しない
- **地域ページのtitleには地名を先頭に残す**（検索クエリ「志木市 ごみ 収集日」対策）
- トップtitle: `ごみニコ｜ごみ収集日・分別方法がすぐわかる無料サイト`
- 志木市title: `志木市のごみ収集日・ごみ分別検索｜ごみニコ`（script.jsが `${city}のごみ収集日・ごみ分別検索｜ごみニコ` で動的生成）
- JSON-LD: name「ごみニコ」+ alternateName「ごみ分別ガイド」（旧名との関連維持・AI検索のエンティティ認識対策）
- AI検索（AIO）方針: 固有名「ごみニコ」を全チャネルで一貫表記し、説明句「ごみ収集日・分別ガイド」を常に併記する

## 2026-07-15セッションの実施内容

1. **ファイル破損の発見と修復**: 作業フォルダの6ファイルが末尾切断されていた。原因は**Claudeのシェル（サンドボックス）マウントの同期不具合**（内容は更新されるがファイル長が古いまま切り詰められる）。git履歴から全ファイル修復済み
2. **お得情報タブを「近日公開」表示に変更**（楽天申請前対応）。旧コンテンツ（引越し見積・不用品回収・楽天グッズ・セール）はコミット7d1b8df以前の履歴から復元可能
3. **トップLP全面リニューアル**（A案: 自作フラットSVGイラスト）
   - セクション見出し24px+英字eyebrow、セクション余白56〜64px、説明文削減
   - ヒーローに自作SVGイラスト（カレンダー+表情つきごみ袋）。Unsplash写真は廃止
   - ごみ種別アイコンを /icons/ SVGに統一（60×60px・角丸14px・背景タイルなし）
   - 自治体リクエストはメールボタン化（**mailtoに件名・本文テンプレートをプリセット済み**。件名「自治体追加のリクエスト」）
   - FAQ「志木市以外にも〜」→「どの自治体で使えますか？」（全国ポータル整合）
   - 締めセクション「毎日のごみ出しを、もっとかんたんに。」（ボタンなし）
   - トップへ戻るボタン（右下固定・600pxスクロールで表示）
4. **DNS・SSL接続完了**（カスタムドメイン・wwwリダイレクト・pages.devリダイレクト。上表参照）
5. **Search Console登録・sitemap送信**（状態は上表参照）
6. **GA4内部トラフィック除外の設定案内**（自宅IP方式+オプトアウトアドオン併用を推奨）
7. **サービス名「ごみニコ」へ一括改称**（全ページ・PWA・sw.jsキャッシュv2化・sitemap lastmod更新）
   - 副次バグ修正: script.jsのcanonical生成が旧 `?city=` 形式だった → `/{city}/` 形式に修正

## 保留中の相談事項（ユーザー未回答）

- **ヒーローのバッジ文言**（現在: 無料/登録不要/アプリ不要）。提案済み案: ①無料/登録不要/インストール不要 ②無料/インストール不要/今すぐ使える ③4つ目に「分別方法もわかる」追加（さんあ〜る等カレンダー専用アプリとの差別化として推奨）
- **締めメッセージ**の代替案（「これ、何ごみ？」に、もう迷わない。／ごみ出しの「うっかり」を、ゼロに。等）
- 「もっと知りたい」セクションの小見出しサイズ（現14px・階層設計上の意図あり）と6色の色分け（ブランド緑1色への統一も提案済み）

## 残タスク（優先順）

1. **Search Consoleのsitemapステータス確認**（1〜2日後。「成功しました」にならなければ再送信）
2. **改称後の再クロール**: `/` と `/shiki/` をURL検査からインデックス登録リクエスト
3. **GA4プロパティ名を「ごみニコ」に変更**（管理→プロパティの詳細。手順案内済み）
4. **楽天アフィリエイト申請**（審査1〜2週間。お得情報タブは近日公開化済みで申請可能な状態）
5. **Google AdSense申請**（審査コードの<head>設置とads.txt設置が必要。コード共有されれば実装可能）
6. 審査通過後: お得情報タブ本実装／広告枠に本番コード差し込み
7. 中期: 検索0件クエリのGA4イベント記録、fuku.svg差し替え、ゆるキャラ検討、Tailwind Play CDN→本番ビルド移行、他自治体追加（和光市・新座市・朝霞市が候補）

## 開発上の注意（重要）

- **ファイル編集は必ずClaudeのファイルツール（Read/Write/Edit）で行うこと。シェル（サンドボックス）経由でこのリポジトリに書き込むと末尾切断破損が起きる**（7/10・7/15に実害確認済み）。シェルは読み取り・検証用途でも内容が古い場合があるため信用しない
- git commit/push はユーザーがローカルで実行する運用（Claudeのシェルからのgit操作は避ける）
- sw.jsを変更したらCACHE_NAMEのバージョンを上げる（既存ユーザーのキャッシュ更新のため。現在 v2）
- 新自治体追加手順は docs/進捗メモ.md のマルチテナント項を参照

## ブランド・デザイン規則

- ブランドカラー: #00A86B（緑）。カテゴリ色: 可燃#E8512A / 不燃#4B72D4 / 資源#00A86B / 粗大#6878A0 / 危険#D96A00
- フォント: H1=30px / セクションH2=24px / eyebrow=13px / カード見出し16〜19px / 本文14〜15px
- タップ領域44px以上、アイコンは /icons/ のSVGを共通使用（60px角丸）
- 文言は「ごみ」（ひらがな）で統一
