{
  "name": "gomi-nico",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build:css": "tailwindcss -i src/styles/global.css -o public/assets/css/tailwind.css --minify",
    "dev": "npm run build:css && astro dev",
    "build": "npm run build:css && astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^7.2.4"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "@tailwindcss/vite": "^4.3.3",
    "tailwindcss": "^4.3.3"
  }
}
